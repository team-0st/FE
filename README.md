# 제로스트 FE

앱인토스 미니앱 (Granite · React Native · TDS). `appName`: `0st`

```bash
npm install
npm run dev:sandbox   # Metro → 샌드박스에 IP:8081, intoss://0st
npm run build         # 0st.ait
npm run typecheck
```

- 콘솔 `appName` = `granite.config.ts` `appName`
- 표기명 = `app.config.ts` `APP_DISPLAY_NAME` (제로스트)
- BE 정렬 컨벤션: [docs/CONVENTIONS.md](docs/CONVENTIONS.md) (API·에러·PR·용어 — **rename 제외**)
