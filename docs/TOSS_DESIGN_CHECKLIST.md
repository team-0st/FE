# 토스·앱인토스 디자인/카피 점검 (제로스트 FE)

> FE 자체 점검용. 최종 심사는 앱인토스·TDS 가이드 기준.

## 1. 말투 (해요체)

| 항목 | 상태 | 비고 |
|------|------|------|
| 안내·버튼·알림 | ✅ | `~해요`, `~보세요`, `~주세요` |
| 경어 혼용 | ✅ | 온보딩 `안녕하세요` — 인사 허용 |
| 개발용 문구 | ✅ | `데모`, `MVP mock` 사용자 노출 제거 (2026-05) |

## 2. 색상·컴포넌트 (TDS)

| 항목 | 상태 | 비고 |
|------|------|------|
| CTA 버튼 | ✅ | `Button type="primary"` (토스 블루) |
| 피드백 | ✅ | `useToast` → `useAppToast` |
| StatCard 힌트 | ✅ | 탭 유도 `blue500`, 안내 `grey600` |
| 브랜드 보라 | ✅ | 탭·슬롯·배너 장식만 |

## 3. 레이아웃·UX

| 항목 | 상태 | 비고 |
|------|------|------|
| 하단 탭 | ✅ | 5탭 (제작·가챠·홈·레시피·마이), `accessibilityRole="tab"` |
| 로딩 | ✅ | `CenterLoader` (TDS Loader), 제작 `loading` 버튼 |
| 홈 넛지 | ✅ | `HomeNudgeBanner` (출석·주간 미션) |
| 네트워크 | ✅ | 출석 API `NETWORK_ERROR` → 토스트 |

## 4. BE 연동 대기 (FE만 준비됨)

- [ ] `postCheckIn` → 실제 fetch
- [ ] 미션 검수·스프 제작 API
- [ ] 토스 푸시 딥링크 (알림 탭 시 홈/미션)
